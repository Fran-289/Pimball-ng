import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pinball.pro',
  appName: 'Pinball NG',
  webDir: 'dist/pinball-ng/browser'
};

export default config;
